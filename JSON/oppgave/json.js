const jobb = {
    name: "oppgaver routiner",
    date: "2026-01-01",
    ansatter: ["alle ansater", "oppgaver"],
};


/* class Peron {
    constructor(name, age, gender) {
        this.name = name;
        this.age = age;
        if(gender) {
            this.gender = gender;
        }
    }
}

const person = new Person("Ludvung", 29);
console.log(person);

const arr = [1, 2, 3];
function testFunction (req1, req2, optional=null) {
    if(optional) {
        if(optional.optional){

        }
    }
}
 testFunction(1, "value", {
    optional1: "val",
    optional2: "val2",
 }); */

/* class animal {
    constructor(birtDate) {
        this.birtDate = birtDate;


    }
    getAge(){
        const today = new Date();
        const birth = new Date(this.birtDate);
        console.log(today - birth);

    }
} 

class Human extends  animal {
    constructor(birtDate) {
        super(birtDate);
    }
}

const person = new Human("2025-23-24");
console.log(person); */